import { describe, expect, it } from 'vitest';
import { normalizeEthereumTransaction, weiToEth } from './ethereum';
const wallet='0x1111111111111111111111111111111111111111';
describe('ethereum adapter helpers',()=>{it('formats wei without floating point loss',()=>{expect(weiToEth('1000000000000000000')).toBe('1');expect(weiToEth('1234567890123456789')).toBe('1.23456789')});it('normalizes incoming value transfers',()=>{const tx=normalizeEthereumTransaction({hash:'0xabc',from:{hash:'0x2222222222222222222222222222222222222222'},to:{hash:wallet},value:'500000000000000000',timestamp:'2026-09-13T00:00:00Z',status:'ok',confirmations:12,fee:{value:'21000000000000'}},wallet);expect(tx.direction).toBe('incoming');expect(tx.amount).toBe('0.5');expect(tx.status).toBe('confirmed')});it('uses method names for zero-value contract calls',()=>{const tx=normalizeEthereumTransaction({hash:'0xdef',from:{hash:wallet},to:{hash:'0x3333333333333333333333333333333333333333',is_contract:true},value:'0',method:'approve',status:'ok'},wallet);expect(tx.direction).toBe('outgoing');expect(tx.amount).toBeUndefined();expect(tx.type).toBe('approve')})});
